import DurationObject from '../duration-object';
import DurationOptions from '../duration-options';
export default function trimZeroValues(units: string[], duration: DurationObject, config: DurationOptions): void;
